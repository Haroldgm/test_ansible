ansible -m setup dev
ansible -m setup -a "filter=ansible_virtualization_type" dev # used to filter the results per host
ansible -m setup -a "filter=ansible_fqdn" dev
ansible -m setup -a "filter=ansible_hostname" dev
